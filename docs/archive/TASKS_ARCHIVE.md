# TASKS archive — completed phases

Verbatim archive of fully-completed sections from `docs/TASKS.md`. **The contract:** when every
item in a section is checked and owner-verified, move the whole section HERE — verbatim, in
order — so the active checklist stays small enough to read in one glance. Same philosophy as
`STATUS_ARCHIVE.md`: history is kept, the working set stays lean.

---
