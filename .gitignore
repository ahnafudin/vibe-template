# Dependencies / build output
node_modules/
dist/
build/
target/

# OS junk
.DS_Store
Thumbs.db

# Env / secrets
.env
.env.*
!.env.example

# Beads / Dolt (kept here up front so `bd init` has little to add; the *.db is
# scoped to .beads/ so a real project database file is never silently ignored)
.dolt/
.beads/**/*.db
.beads-credential-key
.beads/proxieddb/

# Claude Code ephemeral git worktrees
.claude/worktrees/
